import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({ selector: 'app-customer-about', standalone: true, imports: [RouterLink], templateUrl: './about.html' })
export class CustomerAbout {}